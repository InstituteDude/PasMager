# Salin ke .env.production (atau .env) sebelum build:
#   cp .env.example .env.production
#
# Alamat backend yang bisa diakses browser. Untuk setup Tailscale,
# isi dengan MagicDNS name kamu + /api, contoh:
#   VITE_API_URL=https://haekalriz-ideapad.tail2810fa.ts.net/api
VITE_API_URL=
